# AGENTS.md — Read Before Any Frontend Work

**Mandatory pre-read (both):**

1. `src/components/DESIGN.md` — single source of truth for bg (`bg-stone-900`), card (`bg-stone-800` no border), expanded (`bg-[#32302d]`), `Badge.tsx` / `Button.tsx` variants, `Hugeicons` raw + `IOSSpinner`, `toast.tsx` `Sonner`, `sidebar-material` glass, `VADIcon` duotone, motion `0.72s [0.16,1,0.3,1]` layout.
2. `src/components/MISTAKE.md` — 13 real mistakes that made the owner pissed off, with why / what / fix. Do not repeat any.

No frontend edit (including `src/components/Sidebar.tsx`, `src/components/agents/AgentsPage.tsx`, `src/components/onboarding/**`, `src/App.*`) without reading both first. They are the extreme UI consistency contract.
